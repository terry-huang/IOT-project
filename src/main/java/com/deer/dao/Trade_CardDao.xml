<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.deer.dao.Trade_CardDao">
    <insert id="insertTrade_Card">
        insert
        into trade_card(Trade_ID,Card_ID)
        values(#{trade_ID},#{card_ID})
    </insert>
</mapper>